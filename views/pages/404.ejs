<%- include('../partials/header') %>
<%- include('../partials/sidebar') %>

<div class="main-content">
  <% const actionLabel = null; const actionHref = ''; %>
  <%- include('../partials/topbar') %>
  <div class="page-body" style="display:flex; align-items:center; justify-content:center; min-height:60vh">
    <div style="text-align:center">
      <div style="font-family:var(--font-display); font-size:6rem; font-weight:800; color:var(--border2); line-height:1; letter-spacing:-0.05em">404</div>
      <div style="font-size:1.1rem; font-weight:600; color:var(--text); margin:12px 0 6px">Page not found</div>
      <div style="font-size:0.85rem; color:var(--text3); margin-bottom:24px">The page you're looking for doesn't exist.</div>
      <a href="/" class="btn btn-primary">← Back to Dashboard</a>
    </div>
  </div>
</div>
 
<%- include('../partials/footer') %>
